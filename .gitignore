# Byte-compiled / optimized / DLL files
__pycache__/
*.py[cod]
*$py.class

# Environments — NEVER commit real credentials
.env
.venv/
env/
venv/
ENV/
env.bak/
venv.bak/

# .env.example IS safe to commit (contains no real secrets)
!.env.example

# Project Specific — scan reports are user data, not source code
*.xml
!assets/sample_nvd_feed.xml

# OS generated files
.DS_Store
Thumbs.db
